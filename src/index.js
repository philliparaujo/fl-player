import React from "react";
import ReactDOM from "react-dom";
import FileSelector from "./App";

ReactDOM.render(
  <React.StrictMode>
    <FileSelector />
  </React.StrictMode>,
  document.getElementById("root")
);
